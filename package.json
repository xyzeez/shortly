{
  "name": "shortly",
  "version": "1.0.0",
  "license": "ISC",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "npm run prebuild && parcel --open --lazy",
    "prebuild": "rm -rf dist .parcel-cache",
    "build": "npm run prebuild && parcel build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "buffer": "^6.0.3",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "got": "^14.2.1",
    "ldrs": "^1.0.1",
    "validator": "^13.11.0"
  }
}
